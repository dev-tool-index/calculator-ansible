---
# The variables file used by the playbooks in the calculator_service_servers group.
# These don't have to be explicitly imported by vars_files: they are autopopulated.

secret_key1: "{{ vault_secret_key1 }}"
